{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://servoagents.dev/ros2-zephyr/deployment.schema.json",
  "title": "ROS 2 Zephyr fixed deployment",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "name", "domain_id", "node", "rmw", "message_profile", "graph", "endpoints", "resources"],
  "properties": {
    "schema_version": {"const": 2},
    "name": {"type": "string", "pattern": "^[a-z][a-z0-9_]*$"},
    "domain_id": {"type": "integer", "minimum": 0, "maximum": 232},
    "node": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "namespace"],
      "properties": {
        "name": {"type": "string", "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"},
        "namespace": {"type": "string"}
      }
    },
    "rmw": {"type": "array", "minItems": 1, "uniqueItems": true, "items": {"enum": ["rmw_cyclonedds_c", "rmw_zenoh_pico"]}},
    "message_profile": {"const": "fixed_size"},
    "graph": {
      "type": "object",
      "additionalProperties": false,
      "required": ["outbound", "inbound", "api"],
      "properties": {
        "outbound": {"type": "boolean"},
        "inbound": {"type": "boolean"},
        "api": {"type": "array", "uniqueItems": true, "items": {"type": "string"}}
      }
    },
    "endpoints": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "kind", "topic", "type", "qos", "lifetime"],
        "properties": {
          "id": {"type": "string", "pattern": "^[a-z][a-z0-9_]*$"},
          "kind": {"enum": ["publisher", "subscription"]},
          "topic": {"type": "string"},
          "type": {"type": "string", "pattern": "^[A-Za-z][A-Za-z0-9_]*/msg/[A-Za-z][A-Za-z0-9_]*$"},
          "callback": {"type": "string", "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"},
          "lifetime": {"const": "fixed_startup"},
          "qos": {
            "type": "object",
            "additionalProperties": false,
            "required": ["reliability", "durability", "history", "depth"],
            "properties": {
              "reliability": {"enum": ["best_effort", "reliable"]},
              "durability": {"enum": ["volatile", "transient_local"]},
              "history": {"const": "keep_last"},
              "depth": {"type": "integer", "minimum": 1}
            }
          }
        }
      }
    },
    "resources": {
      "type": "object",
      "additionalProperties": false,
      "required": ["local_endpoints", "remote", "workers", "application", "targets"],
      "properties": {
        "local_endpoints": {"type": "integer", "minimum": 0},
        "remote": {
          "type": "object",
          "additionalProperties": false,
          "required": ["participants", "nodes", "endpoints"],
          "properties": {
            "participants": {"type": "integer", "minimum": 0},
            "nodes": {"type": "integer", "minimum": 0},
            "endpoints": {"type": "integer", "minimum": 0}
          }
        },
        "workers": {
          "type": "object",
          "additionalProperties": false,
          "required": ["maximum"],
          "properties": {
            "maximum": {"type": "integer", "minimum": 1}
          }
        },
        "application": {
          "type": "object",
          "additionalProperties": false,
          "required": ["static_reserve_bytes"],
          "properties": {
            "static_reserve_bytes": {"type": "integer", "minimum": 0}
          }
        },
        "targets": {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": {
            "type": "object",
            "additionalProperties": false,
            "required": ["flash_bytes", "ram_bytes"],
            "properties": {
              "flash_bytes": {"type": "integer", "minimum": 1},
              "ram_bytes": {"type": "integer", "minimum": 1}
            }
          }
        }
      }
    }
  }
}
